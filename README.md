# Redis Sentinel Test

Docker image designed to test Redis Sentinel on Kubernetes. For example, Bitnami Redis Helm chart.

It tests ping, writing and deleting a key-value in a set.

To deploy as a job. If test was successful job as well.